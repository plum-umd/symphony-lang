Msort (ints, 16 bit)
  BGV
    Cond(S,S,S)
    S ≤ S

Qsort (reals, binary64 as per iee08)
  BGW, SPDZ
    Cond(S,S,S)
    S ≤ S
    S + S
    S - S

GCD/Euclid (ints, 4096 bit)
  Yao, BGV
    S ≡ const
    Cond(S,S,S)
    S % S
    S < S

Karmakar (reals, binary64 as per iee08)
  SPDZ
    Cond(S,const,S)
    sqrt S
    S ≡ const
    S + S
    S + const   -- ?
    S * S
    S * const   -- ?
    S / S
    -- (↓ Low priority (only needed if early returns are required))
    Cond(S,S,S)
    S ≤ S

DB-Stats (unsigned ints, 32 bit | reals, binary64 as per iee08)
  BGV
    Cond(S,S,S) (unsigned ints, reals)
    S ≡ S       (unsigned ints)
    S < S       (unsigned ints)
    S - S       (unsigned ints, reals, looks like it might not be possible to underflow)
    S + S       (unsigned ints, reals)
    S * S       (unsigned ints, reals)
    S / S       (unsigned ints to reals, reals)
    sqrt S      (reals)
    to_flt      (unsigned int to real)

ATQ (ints, 32 bit)
  BGW, SPDZ
    Cond(S,S,S) (ints, bools, and principals)
    S ≡ S       (ints, bools, and principals)
    S ≤ S       (for sorting a list of ints)
    S % 2       (or is-even)

