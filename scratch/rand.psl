principal A B

def main : 𝟙 → int{A,B}
def main • =
  let x = rand int{yao:A,B}
  let y = rand int{yao:A,B}
  let z = x + y
  let a = rand-range int{gmw:A,B} (0,10)
  let b = rand-range int{gmw:A,B} (-10,0)
  let c = a + b
  in 
  reveal{A,B} z , reveal{A,B} c
