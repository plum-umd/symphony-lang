principal A
principal B

def main () =
  let a = share{ spdz : A } 0 in
  let l = { par : A, B } ((share{ spdz : A } 0) :: [ ]) in
  let l' = { par : A, B } (a :: [ ]) in
  l'

def expected = {par:A,B} (share{spdz:A} 0) :: []

