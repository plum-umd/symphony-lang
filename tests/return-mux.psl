principal A B

def main • =
  par {A,B}
  proc
  let x = share {yaoN:A → A,B} false
  let y = (share {yaoN:B → A,B} 3, share {yaoN:B → A,B} 4)
  let z = (share {yaoN:B → A,B} 4, share {yaoN:B → A,B} 1)
  let sum =
    mux if share {yaoN:A → A,B} false
    then L 1
    else R 2
  do mux case sum
       { L x → return x + 10
       ; R y → return y + 100
       }
  in 0

def expected = par {A,B} share {yaoN:A → A,B} 102
