principal A B

def main • =
  par {A,B}
    let x = share{yao, 𝟙 + 𝟙 : A → A,B} L • in
    mux case x
      { L • → 1
      ; R • → 2
      }

def expected = par {A,B} share{yao, ℤ : A → A,B} 1
