principal A B

def main • =
  par {A,B}
  proc
  do mux if share {yao, 𝔹 : A → A,B} true
    then
      when true
      then return 100
    else
      when true
      then return 200
  in 500

def expected = par {A,B} share {yao, ℤ : A → A,B} 100
