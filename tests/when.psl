principal A B

def main • =
  par {A,B}
  proc
  do mux if share {yaoN:A → A,B} true
    then
      when true
      then return 100
    else
      when true
      then return 200
  in 500

def expected = par {A,B} share{yaoN:A → A,B} 100
