principal A B C

-- Fails because they are in different protocols
def main : (unit@all -> int@{A})@all
def main () =  par {A, B, C} (let x = 1 in let y = (share [gmw, int : A -> {A, B,C}] (par {A} 2) )  in (reveal [yao, int : {A,B,C} -> A] (par {A, B, C} (x + y)))) 