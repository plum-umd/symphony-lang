principal A
principal B
principal C

def main () =
  let x = { par : B } 0 in
  let y = { par : A } L x in
  let z = reveal{ A, B } y in
  { par : B }
    case z
    { L i -> i + 1
    ; R _ -> 0
    }
