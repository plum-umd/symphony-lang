principal A B

def main • =
  par {A,B}
    let x = share{yao:A → A,B} (par {A:𝔹} true) in
    let z = share{yao:A → A,B} (par {A:𝔹} false) in
    let y = share{yao:B → A,B} (par {B:𝔹} false) in
    reveal {yao:A,B → A,B} (mux if (x || false) then (true && y) else z)
